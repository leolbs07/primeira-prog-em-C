#include <stdio.h>
#include <locale.h>

int main() {
	setlocale(LC_ALL, "");
	int i, soma;
	
    soma = %d + %d + %d + %d + %d;
    
    printf("Informe ao programa 5 números inteiros: \n");
    scanf("%d",&soma);

return 0;
}
